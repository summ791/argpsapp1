name: Keep Supabase Awake

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Supabase health endpoint
        run: |
          curl -s https://bnqsnqumzywxttzojvtk.supabase.co/rest/v1/ \
          -H "apikey: ${{ secrets.SUPABASE_ANON_KEY }}"
